# 首次使用与任务流程 · 1.5

本指南以 BOSS 直聘、智联招聘的 Chrome Bridge 主流程为准。先按 [Windows 指南](WINDOWS_SETUP.md) 启动工作台并加载扩展；应用为 1.5.0，配套扩展为 1.8.0。

## 1. 准备一个求职档案

在 Chrome 打开 `http://127.0.0.1:6866`。先到「环境配置」设置模型连接，再到「AI配置」新建或选择档案。

- 写明目标岗位、工作地点和不考虑的方向，避免只凭能力分数判断求职意愿。
- 可以直接粘贴简历文本。上传 PDF、Word 或图片需先准备 [本地识别器](resume-parser/README.md)；TXT 不需要 Docling。
- 文件识别只产生预览。核对内容后，点击确认保存并生成配置；生成配置会调用你设置的模型。
- 每次切换档案后，先确认页面显示的档案名称与资料一致。简历、配置及分析数据保存在本地数据库中。

本机文件识别与后续 AI 分析是两件事：前者使用本地解析器，后者可能向外部模型发送简历和岗位内容。

## 2. 设置搜索条件并登录平台

进入「Boss直聘」或「智联招聘」，设置关键词、城市、薪资、经验、学历等。各平台支持的筛选项不同，以页面实际选项为准。

Chrome Bridge 复用 Chrome 中的平台页面和登录状态。先在官网完成登录或验证，再回到工作台检查状态；不需要为了主流程把 Cookie 粘贴到公开页面、Issue 或文档。

BOSS 实际投递前，需要在 BOSS App 关闭平台自带打招呼语，并在工作台确认该设置，避免默认话术抢先发送。只采集和分析时，不要误点确认投递。

## 3. 采集岗位，等待分析

启动扫描后，扩展读取列表和详情，将岗位交给本地后端，AI 任务再分析简历与岗位的匹配程度。查看进度、失败原因和待处理状态；进入页面或读取到列表不代表整轮扫描已经完成。

遇到登录失效、安全验证或官网结构变化时，在 Chrome 中处理，再按页面提示继续。不要反复启动同一任务。

| 状态 | 你应该怎样理解 |
| --- | --- |
| 已采集待分析 / AI分析中 | 数据已经进入处理流程，还没有最终分析结果 |
| 待确认 | 候选岗位等待你审阅，尚未实际投递 |
| AI不匹配 | 分析不推荐，仍可查看理由 |
| AI分析失败 | 模型调用或解析出错，查看错误后再决定是否重试 |
| 采集信息不足 | 缺少必要岗位字段，不能当作完整结果 |
| 已过滤 | 不满足筛选条件或命中黑名单 |

## 4. 核对匹配依据与岗位

在「智联分析」可以查看当前保存的简历、下次扫描关键词和确认阈值；也可以切换全部岗位与当前扫描范围。岗位列表按岗位、薪资要求、匹配分析、投递状态和操作组织。

![智联分析中的匹配依据，虚构演示数据](docs/images/screenshots/v1.5-analysis-basis.jpg)

- AI 评分表示能力证据，不代表你愿意接受该岗位；结合方向、薪资、工作安排与风险判断。
- 修改简历或关键词不会自动重算历史评分。页面中的当前简历不代表历史分析当时使用的版本。
- 展开匹配依据和岗位详情，必要时打开官网原岗位核对。
- 不考虑的岗位可以标记「不感兴趣」，保留记录并移出待确认。
- 图表在分析页底部，按需展开。

## 5. 检查话术，再确认投递

在 BOSS 或智联分析页查看沟通草稿，按实际经历修改，确认最终文本。

单个或批量确认都会核对最终话术及任务状态，再创建投递任务。确认前先检查岗位、当前档案与数量。模型生成的文字不应包含未经证实的经历、承诺或薪资条件。

确认后由 Chrome Bridge 在平台页面执行，并回传结果；确认按钮被点击不等于投递已经成功。

## 6. 核对结果与异常

| 状态 | 后续动作 |
| --- | --- |
| 已投递 | 查看已写回的结果；避免重复操作 |
| 投递确认中 | 等待执行结果，不要立即再次点击 |
| 投递结果待确认 | 核对官网是否已经执行，使用页面提供的对账方式；不要盲目重试 |
| 投递失败 | 查看登录、按钮、网络或平台限制等具体原因，再处理 |
| 已跳过 | 保留历史记录，不在待确认队列中 |

系统会保护已投递状态，重新分析不会把它重置成未投递。遇到验证码、账号限制或页面变化，先人工处理，不会绕过平台限制。

## 诊断入口

- 页面和 API：`http://127.0.0.1:6866`；就绪检查：`/api/ready`。
- 一键启动输出：`logs/windows-unified.log`；默认应用日志：`target/logs/get-jobs.log`。托管服务可能设置其他路径，以管理器配置为准。
- Chrome Bridge 无响应：检查扩展 1.8.0、地址、页面刷新和官网登录。
- AI 分析失败：检查模型配置、网络及错误信息。重试可能再次调用模型服务。
- 数据为空：确认当前档案、关键词、扫描范围和筛选条件。

向 Issue 提供错误时删除密钥、Cookie、个人资料和真实对话。开发模式的 8888 端口仅适用于 [显式双端口配置](docs/development/setup.md)，日常统一服务仍为 6866。

## 实验平台与开发者说明

猎聘 / 51job 保留旧 Playwright 适配器，侧栏入口禁用，普通启动默认只读采集。其真实投递需要额外明确确认，尚不能当成与 BOSS / 智联相同的完整验收能力。

BOSS 后端 `/api/boss/start` 已禁用；主流程由 Chrome Bridge 提交到 `/api/boss/chrome/jobs`。智联主流程同样使用 `/api/zhilian/chrome/jobs`。旧接口存在不代表它就是推荐入口。实际能力声明可从 `/api/platforms/capabilities` 查看。
