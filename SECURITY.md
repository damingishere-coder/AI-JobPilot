# 安全说明

## 基本原则

投递牛马默认作为个人电脑上的本地工具使用，不建议部署到公网服务器。代码仓库只保存源码、迁移脚本和文档，不保存个人账号数据。

不要提交：

- 账号、密码、Cookie、Token、API Key。
- `.env`、`.env.local`。
- 本地 SQLite 数据库和备份文件。
- 浏览器用户数据目录、Chrome 缓存、Playwright 缓存。
- 简历图片、聊天截图或其他个人敏感资料。

## Chrome Bridge 安全边界

- Chrome Bridge 仅面向本地前端和已声明的招聘平台域名工作。
- 扫描岗位时，扩展只把结构化岗位信息提交到本地后端。
- 真实投递必须经过“待确认”状态和用户点击确认。
- 扩展不应把平台 Cookie、账号密码或浏览器缓存写入仓库。

## 数据库安全

- 默认数据库路径为 `db/getjobs.db`，该目录被 `.gitignore` 忽略。
- Flyway 脚本只管理表结构，不包含个人数据。
- 旧数据库通过 baseline 兼容，不会因为本轮迁移删除用户数据。

## CI 安全

- GitHub Actions 使用临时 SQLite 文件，不读取本机 `db/getjobs.db`。
- CI 不上传构建产物和日志。
- CI 不需要个人招聘平台账号、Cookie 或模型 API Key。

## 后续功能安全要求

AI 代聊 HR、微信提醒、面试信息汇总等能力上线前，需要先明确：

- 哪些动作只是提醒，哪些动作会代表用户发送内容。
- 发送给 HR 的内容是否必须人工确认。
- 微信、企业微信、AI 模型配置如何避免泄露。
- 平台规则是否允许该类自动化辅助。
