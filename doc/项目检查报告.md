# 项目检查报告

检查时间：2026-05-27

## 项目判断

这是一个本地运行的求职自动化 GUI 项目，整体由三部分组成：

- 后端：Spring Boot 负责 API、配置、数据库、任务调度和进度推送。
- 前端：Next.js 提供图形化配置、运行控制和岗位分析页面。
- Worker：Java + Playwright 负责实际打开招聘网站并执行自动化操作。
- Chrome Bridge：本地 Chrome 扩展负责复用用户已登录标签页进行 Boss/智联岗位扫描和确认投递。

当前项目已作为本地 Git 仓库维护，阶段命名为 `1.2 MVP 全流程打通`。

## 技术栈确认

| 模块 | 技术 |
| --- | --- |
| 后端 | JDK 21、Spring Boot 3.5.7、Gradle、MyBatis-Plus |
| 前端 | Next.js 16、React 19、TypeScript、Tailwind CSS |
| 自动化 | Microsoft Playwright for Java 1.51.0 |
| 浏览器桥接 | Chrome Extension Manifest V3 |
| 数据库 | SQLite |
| 运行端口 | 后端 `8888`，前端 `6866` |
| 当前版本 | 后端 `1.2.0`，前端 `1.2.0`，Chrome 扩展 `1.2.0` |

## 主要目录

| 路径 | 说明 |
| --- | --- |
| `src/main/java/com/getjobs/application` | Spring Boot 应用层，包含 controller、service、mapper、entity 和配置 |
| `src/main/java/com/getjobs/worker` | 招聘平台自动化逻辑 |
| `chrome-extension` | Chrome Bridge 扩展，包含 Boss/智联 content script |
| `src/main/resources/application.yaml` | 后端配置 |
| `front/app` | Next.js 页面 |
| `front/server.config.js` | 前端端口和 API 地址配置 |
| `db/getjobs.db` | 本地 SQLite 数据库 |
| `doc` | 文档目录 |

## 支持平台

当前代码中包含以下平台：

- Boss 直聘
- 猎聘
- 前程无忧 51job
- 智联招聘

README 原文曾提到拉勾，但当前代码目录和 controller 中未看到拉勾模块，因此新文档按当前代码实际情况整理为四个平台。

1.2 版本中，Boss 和智联已打通 Chrome Bridge MVP 路线：扫描岗位后交给后端 AI 分析，命中岗位进入“待确认”，用户在分析页确认后再投递，并将结果回写到分析列表。

## 启动方式

后端：

```bash
./gradlew bootRun
```

前端：

```bash
cd front
pnpm install
pnpm dev
```

访问地址：

```text
http://localhost:6866
```

后端健康检查：

```text
http://localhost:8888/api/health
```

## 文档整理结果

本次整理后新增或重写了以下文档：

| 文件 | 说明 |
| --- | --- |
| `README.md` | 重写为项目入口，保留快速启动、文档导航和关键注意事项 |
| `doc/1.0自动投简历脚本.md` | 阶段说明文档，记录 1.2 MVP 全流程、当前进度、平台状态和后续计划 |
| `doc/文档索引.md` | 汇总所有文档并标注用途 |
| `doc/使用指南.md` | 面向使用者的完整运行和配置流程 |
| `doc/开发指南.md` | 面向开发者的结构、命令和扩展说明 |
| `doc/架构说明.md` | 当前架构、模块职责和运行链路 |
| `doc/API接口.md` | 按 controller 整理主要 REST 和 SSE 接口 |
| `doc/项目检查报告.md` | 本文件，记录项目检查结论 |

原有文档没有删除，文章类和早期草案类内容都保留在 `doc` 目录，并在 `文档索引.md` 中标注了用途。

## 注意点

- `db/getjobs.db` 存在于当前文件夹，但 `.gitignore` 已忽略 `db/` 和 `*.db`。分享项目时建议删除或替换本地数据库。
- `.pnpm-store/` 曾误进入暂存区，本次已从 Git 索引移除，并补充到 `.gitignore` 与 `.dockerignore`。
- `Dsign.md` 是早期概要设计草案，其中部分包名、接口路径和未来规划与当前代码不完全一致。
- `Detail.md` 中有 Maven、JDK17 图片等旧内容，而当前后端实际使用 Gradle 和 JDK 21。
- 招聘平台页面结构变化会影响 Playwright 定位器，后续维护时优先检查 `worker/<platform>/Locators.java` 和平台执行类。
- Chrome Bridge 页面结构变化时，优先检查 `chrome-extension/boss-content.js` 和 `chrome-extension/zhilian-content.js`。
- 项目适合本机运行，不建议服务器部署。

## 后续建议

- 如需发布给普通使用者，可以保留新的 README 和使用指南，隐藏或移走文章类文档，降低阅读噪音。
- 如要增加平台或修复平台投递问题，应同步更新 `API接口.md`、`架构说明.md` 和 `使用指南.md`。
- 如要继续稳定 1.2，可增加更多端到端实测记录，按平台记录采集数量、待确认数量、投递成功数和失败原因。
